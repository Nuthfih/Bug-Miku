# Bug-Miku

Code are made with AI.

**STILL UNDER CONSTRUCTION**

### How to Run:
1. Make sure you have installed Python.
2. Open your terminal.
3. CD into this folder.
4. Run the command:
   ```bash
   python BugAnimation.py
   ```

### Note:
You may need to install `pygame` first. You can do this by running:
```bash
pip install pygame
```
*(Alternatively, you can use `pip install -r requirements.txt`)*

---
Project is Inspired by: [World.Execute(me); by Kritzkingvoid](https://github.com/Kritzkingvoid/Execute.SingAlone-Me-/tree/master)